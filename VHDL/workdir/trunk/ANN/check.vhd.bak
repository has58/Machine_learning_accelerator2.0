Library IEEE;
use IEEE.std_logic_1164.ALL;
use std.textio.ALL;

use work.ANN_Lib.All;

Entity check is
	port(clk : in bit;
		  sig_out : out control_signal);
	end entity;
	
	
Architecture check_arch of check is
	for CU : control_unit_comp use entity work.control_unit(control_unit_arch);
	begin
			CU port map ()
	end architecture;